module.exports = {
    commands: ["queue"],
    callback: (message, arguments, text) => {
        // TODO: Make embed format
        // TODO: Default is to show next item in order, but make an option to show a list
    },
};