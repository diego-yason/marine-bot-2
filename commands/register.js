/*
Required parameters:
commands
callback
*/

module.exports = {
    commands: ["register"],
    callback: (message, arguments, text) => {
        // code
    },
    expectedArgs: "",
    permissionError: "",
    minArgs: 0,
    maxArgs: null,
};